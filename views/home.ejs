<!DOCTYPE html>
<html>
<head>
  <!-- Embedded CSS styling to define card background color -->
  <style>
    .card {
      background-color: lightblue;
    }
  </style>
</head>
<body>
  <!-- Your HTML code here -->
  <div class="mb-5 p-5">
    <!-- Loop through projects and display project information -->
    <% for( let project of projects ) { %>
      <div class="card mb-2 w-50 m-auto">
        <div class="card-header">
          <strong> Name of the project : </strong>
          <%= project.name %>
        </div>
        <div class="card-body">
          <h6 class="card-subtitle mb-2 text-muted">
            <strong> Author : </strong>
            <%= project.author %>
          </h6>
          <p class="card-text">
            <strong> Description : </strong>
            <%= project.description %>
          </p>
          <!-- Link to open the project with a "Open project" button -->
          <div class="m-auto text-end">
            <a href="/project/<%=project._id%>" class="btn btn-primary m-auto">Open project</a>
          </div>
        </div>
      </div>
      <% } %>
  </div>
</body>
</html>
